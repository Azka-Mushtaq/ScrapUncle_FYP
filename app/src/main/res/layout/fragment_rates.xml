
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".RatesFragment">

    <!-- Search Bar -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardElevation="4dp"
        app:cardCornerRadius="8dp"
        app:cardBackgroundColor="@color/white"
        >
        <EditText
            android:id="@+id/search_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Search for an item"
            android:drawableStart="@drawable/ic_search"
            android:drawablePadding="8dp"
            android:background="@color/transparent"
            android:padding="12dp"
            />
    </androidx.cardview.widget.CardView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="RECYLABLES"
        android:textStyle="bold"
        android:textSize="18sp"
        android:padding="8dp" />
    <!-- ListView for Recyclables -->
    <ListView
        android:divider="@color/transparent"
        android:dividerHeight="0dp"
        android:id="@+id/recyclables"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="0dp"
        tools:listitem="@layout/list_item"
       />

    <!-- Large Appliences-->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LARGE APPLIANCES"
        android:textStyle="bold"
        android:textSize="18sp"
        android:padding="8dp" />
    <!-- ListView for Recyclables -->
    <ListView
        android:divider="@color/transparent"
        android:dividerHeight="0dp"
        android:id="@+id/large_appliances"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="0dp"
        tools:listitem="@layout/list_item"
        />


    <!-- Small Appliences
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SMALL APPLIANCES"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="8dp" />
    <!- ListView for Recyclables
    <ListView
        android:id="@+id/small_appliances"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="0dp"
        tools:listitem="@layout/list_item"
        />

    <!- Mobile & Computers
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LARGE APPLIANCES"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="8dp" />
    <!- ListView for Recyclables
    <ListView
        android:id="@+id/mobile_and_computers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="0dp"
        tools:listitem="@layout/list_item"
        />

    <!- Others
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="OTHERS"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="8dp" />
    <!- ListView for Recyclables
    <ListView
        android:id="@+id/others"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="0dp"
        tools:listitem="@layout/list_item"
        />
    -->
</LinearLayout>
